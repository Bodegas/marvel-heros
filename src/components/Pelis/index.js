import React from "react";
import PropTypes from "prop-types";

const Pelis = ({ props }) => {
  return <h1>Pelis a tutiplén</h1>;
};

Pelis.displayName = "Pelis";
Pelis.propTypes = {};

export default Pelis;
